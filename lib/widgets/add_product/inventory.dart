import 'package:flutter/material.dart';

class Iventory extends StatelessWidget {
  const Iventory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('inventory'),),
    );
  }
}
